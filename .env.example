# ==============================================================================
# WealthArena Environment Configuration
# ==============================================================================
# Copy this file to .env and fill in your actual values
# Do NOT commit .env to version control!

# ==============================================================================
# LLM Configuration (Required)
# ==============================================================================
GROQ_API_KEY=your_groq_api_key_here
GROQ_MODEL=llama3-8b-8192
LLM_PROVIDER=groq

# ==============================================================================
# Machine Learning Models
# ==============================================================================
SENTIMENT_MODEL_DIR=models/sentiment-finetuned
ENABLE_SENTIMENT_ANALYSIS=false

# ==============================================================================
# Application Configuration
# ==============================================================================
APP_HOST=0.0.0.0
APP_PORT=8000
# PORT is used by Azure App Service (automatically set in production)
# For local development, APP_PORT is used if PORT is not set

# ==============================================================================
# Data Storage
# ==============================================================================
CHROMA_PERSIST_DIR=data/vectorstore

# ==============================================================================
# RAG Data Pipeline Configuration
# ==============================================================================

# News Scraping Configuration
NEWS_SCRAPE_INTERVAL_MINUTES=30
NEWS_ARTICLES_PER_FEED=10

# RSS Feed Configuration
# RSS feed URLs are configured in the code (app/tools/news_ingest.py)
# and have been updated to use working alternatives for Reuters (via Google News RSS),
# CNN Money, and Yahoo Finance as of November 2025.

# Investopedia Scraping Configuration
# Educational articles from Investopedia on trading topics
INVESTOPEDIA_SCRAPE_INTERVAL_HOURS=24
INVESTOPEDIA_CATEGORIES=technical-analysis,fundamental-analysis,risk-management,trading-strategies,forex,options
INVESTOPEDIA_ARTICLES_PER_CATEGORY=10
ENABLE_INVESTOPEDIA_INGESTION=true

# Forex Factory Scraping Configuration
# Economic calendar events and high-impact forex news
FOREXFACTORY_SCRAPE_INTERVAL_HOURS=6
FOREXFACTORY_DAYS_RANGE=7
FOREXFACTORY_MIN_IMPACT=medium
ENABLE_FOREXFACTORY_INGESTION=true
ENABLE_SELENIUM_FALLBACK=false

# Reddit Scraping Configuration
# Community discussions from trading subreddits
# NOTE: Reddit scraping is OPTIONAL. The pipeline will gracefully skip it if credentials
# are not configured. You can leave the placeholder values if you don't want Reddit data,
# and the system will log a warning but continue normally.
REDDIT_CLIENT_ID=your_client_id_here
REDDIT_CLIENT_SECRET=your_client_secret_here
REDDIT_USERNAME=your_reddit_username
REDDIT_PASSWORD=your_reddit_password
REDDIT_USER_AGENT=python:com.wealtharena.reddit-scraper:v1.0.0 (by /u/your_username)
REDDIT_SUBREDDITS=wallstreetbets,stocks,options,investing,Daytrading,pennystocks,forex,algotrading
REDDIT_SCRAPE_INTERVAL_MINUTES=30
REDDIT_POSTS_PER_SUBREDDIT=50
REDDIT_MIN_SCORE=10
ENABLE_REDDIT_INGESTION=true

# Ingestion Control
ENABLE_NEWS_INGESTION=true

# Minimum Document Thresholds (for deployment verification)
MIN_DOCUMENTS_NEWS=50
MIN_DOCUMENTS_EDUCATIONAL=20
MIN_DOCUMENTS_FOREX=10
MIN_DOCUMENTS_COMMUNITY=30

# ==============================================================================
# SENTRY_DSN=
# ==============================================================================

# ==============================================================================
# Azure App Service Deployment Settings
# ==============================================================================
# IMPORTANT: These settings are managed by the deployment script
# Do NOT manually set these in Azure App Service configuration

# SCM_DO_BUILD_DURING_DEPLOYMENT=true
# Required for Oryx to run pip install during ZIP deployment
# The deployment script automatically sets this

# PYTHONUNBUFFERED=1
# For proper logging in Azure (output is not buffered)
# The deployment script automatically sets this

# IMPORTANT: Do NOT set WEBSITE_RUN_FROM_PACKAGE=1 for Python apps
# It bypasses Oryx build and prevents pip install from running
# The deployment script automatically removes this setting if present

# ENABLE_ORYX_BUILD is not needed for ZipDeploy (ignored by Azure)
# Only SCM_DO_BUILD_DURING_DEPLOYMENT matters for ZIP deployments

# ==============================================================================
# Local Development Notes
# ==============================================================================
# For local development:
# - Use APP_HOST and APP_PORT (or PORT) for server configuration
# - Set GROQ_API_KEY to your actual API key
# - CHROMA_PERSIST_DIR is where vector database files are stored locally
# - SENTIMENT_MODEL_DIR should point to your trained model directory

# ==============================================================================
# Azure Production Notes
# ==============================================================================
# In Azure App Service:
# - PORT is automatically set by Azure (use $PORT in startup command)
# - Do NOT set WEBSITE_RUN_FROM_PACKAGE (blocks Oryx build)
# - SCM_DO_BUILD_DURING_DEPLOYMENT must be true for dependencies to install
# - The deployment script (deploy-appservice-repair.ps1) handles all Azure-specific settings
