# WealthArena Backend Environment Configuration Template
# IMPORTANT: Copy this file to .env and fill in actual values
# This file should NEVER contain real credentials

# ============================================================================
# Local Development (Simplified Setup)
# ============================================================================
# For quick local testing without database installation:
# - Set USE_MOCK_DB=true (in-memory user storage)
# - No DB credentials needed
# - Run master_setup_simplified.ps1 for automated setup
# ============================================================================

# Database Configuration (ignored when USE_MOCK_DB=true)
# For production/real DB, set USE_MOCK_DB=false and configure below:
# 
# Production/Real DB Example (Azure SQL):
DB_HOST=sql-wealtharena-dev.database.windows.net
DB_NAME=wealtharena_db
DB_USER=wealtharena_admin
DB_PASSWORD=WealthArena2024!@#$%
DB_PORT=1433
DB_ENCRYPT=true

# Azure Storage (optional for Phase 1)
AZURE_STORAGE_ACCOUNT=stwealtharenadev
AZURE_STORAGE_CONNECTION_STRING=your-storage-connection-string

# Azure Cosmos DB (optional - backend doesn't use this yet)
AZURE_COSMOS_ACCOUNT=cosmos-wealtharena-dev
AZURE_COSMOS_CONNECTION_STRING=your-cosmos-connection-string

# Server Configuration
PORT=3000
NODE_ENV=development

# Mock Database Mode (for local development - no real DB required)
USE_MOCK_DB=true
# Set to false for production/real database usage
# When USE_MOCK_DB=true:
#   - All user data stored in memory (cleared on server restart)
#   - No database installation or credentials needed
#   - Perfect for local testing and development
# When USE_MOCK_DB=false:.\
#   - Requires real database (PostgreSQL, SQL Server, or Azure SQL)
#   - Configure DB_HOST, DB_NAME, DB_USER, DB_PASSWORD above

# Redis Configuration (optional for Phase 1)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# JWT & Security
JWT_SECRET=wealtharena-jwt-secret-change-this-in-production-12345
JWT_EXPIRES_IN=7d
BCRYPT_ROUNDS=10

# OpenAI API (for AI features)
OPENAI_API_KEY=your-openai-api-key-here

# Alpha Vantage API (for market data)
ALPHA_VANTAGE_API_KEY=your-alpha-vantage-api-key-here

# Chatbot API Configuration
# URL of the chatbot service (Python FastAPI service)
# Should be running on port 8000 by default
CHATBOT_API_URL=http://localhost:8000

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:5001,http://localhost:8081,http://localhost:3000

# API Configuration
API_VERSION=v1

# Automatic Data Loading Configuration
# Set to false to disable automatic CSV data loading on backend startup
# Default: true (automatic loading enabled)
AUTO_LOAD_DATA=true

# Skip automatic data loading (alternative to AUTO_LOAD_DATA=false)
# Set to true to skip automatic data loading regardless of AUTO_LOAD_DATA setting
# Useful for debugging or manual control
SKIP_AUTO_LOAD=false

