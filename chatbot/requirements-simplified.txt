# WealthArena Simplified Dependencies
# Minimal requirements for DeepSeek API-only chatbot (no RAG, no vector DB)

# Core FastAPI dependencies
fastapi==0.104.1
uvicorn==0.24.0
python-dotenv==1.0.0
httpx>=0.27.0
pydantic>=2.0.0
websockets==12.0

# Market data (for market endpoints)
yfinance==0.2.18

# Metrics
prometheus-client==0.19.0

