# ---- Azure SQL ----
# ⚠️ SECURITY: Copy this file to sqlDB.env and fill in your actual values.
# Store actual secrets in Azure Key Vault or CI/CD secrets, not in version control.
# SERVER should be just the server name without port, or use format: server,port (comma, no space)
SQL_SERVER=your-sql-server.database.windows.net
SQL_DB=your_database_name
SQL_UID=your_username
SQL_PWD=your_password_here

# ---- ADLS (access key or SAS – pick one) ----
# ⚠️ SECURITY: Real secrets should not be in version control.
# Store actual secrets in Azure Key Vault or CI/CD secrets.
AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=your_account_name;AccountKey=your_account_key_here;EndpointSuffix=core.windows.net
AZURE_UPLOAD=true
AZURE_STORAGE_FILESYSTEM=raw
AZURE_PREFIX=asxStocks

# ---- Processing Configuration ----
ADLS_CONTAINER=raw
ADLS_RAW_PREFIX=asxStocks
BATCH_ROWS=75000
MERGE_EVERY_ROWS=200000
TRUNCATE_PRICES_BEFORE=false
# ⚠️ SAFETY: Deletion is opt-in only. Set to true only after verifying MERGE results.
ADLS_DELETE_SOURCE=false

# ---- ODBC Driver Configuration ----
# Ensure ODBC Driver 18 for SQL Server is installed
# Check installation: odbcad32.exe (look in Drivers tab)
# DRIVER={ODBC Driver 18 for SQL Server}

